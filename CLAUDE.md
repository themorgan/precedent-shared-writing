<!-- Claude Code adapter: CLAUDE.md is the file Claude Code auto-loads; the
     canonical instructions live in AGENTS.md (harness-neutral). The @import
     below pulls AGENTS.md into context natively — keep repo-specific content
     in AGENTS.md, not here. Install this file at the repo root.

     A practice SOURCE set installs it too, and until 2026-09-20 none did.
     Do NOT add a second @import for .precedent/SESSION_PRACTICES.md: a
     SessionStart hook renders and injects that file, and importing it here
     loads a stale copy, or a duplicate one. Both points:
     spec/PACK_SESSION_DOES_NOT_LOAD_UNIVERSAL.md. -->

@AGENTS.md
